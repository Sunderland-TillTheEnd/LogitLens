# logit_lens_hf.py 与 chat.py 的运行依赖（Python 3.14+）
# GPU 版 PyTorch 请按 https://pytorch.org 的 CUDA 指引安装，例如：
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.1
transformers>=4.45
safetensors>=0.4
numpy>=1.24
matplotlib>=3.8
pillow>=10.0
